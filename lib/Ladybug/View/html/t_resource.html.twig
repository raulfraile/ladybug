{% extends "_base.html.twig" %}

{% block type %}
    {{ parent() }}({{ var.resourceType }})
{% endblock %}

{% block value %}
    {% if var.isCustomData %}
        {{ m.array_block('resource (' ~ var.resourceType ~ ')', var.resourceCustomData, level, false) }}
    {% endif %}
{% endblock %}
