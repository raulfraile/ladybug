{% import "_macros.cli.twig" as m %}

{% spaceless %}

    {{ m.array_key(array_key) }}

    <t_array>array({{ length }})</t_array><intro>

    {% for key,var in value %}
        {{ m.indent(level) }}
        {{ var.render(key, 'cli')|raw }}
    {% endfor %}

{% endspaceless %}