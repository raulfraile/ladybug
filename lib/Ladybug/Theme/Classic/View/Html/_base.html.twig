{% import "_macros.html.twig" as m %}

{% spaceless %}
<div class="final">
    {{ m.array_key(array_key) }}
    <span class="type">{% block type %}{{ var.type }}{% endblock %}</span>
    <span>{% block value %}{{ var.formattedValue }}{% endblock %}</span>
</div>
{% endspaceless %}
