{% import "_macros.text.twig" as m %}

{{ m.array_key(key) }}

array({{ var.length }}) <intro>

{% for item in var.value %}
    {{ m.indent(level) }}
    {{ render_type(item.value, item.key) }}<intro>
{% endfor %}