{% extends "_base.text.twig" %}

{% block type %}array({{ var.length }}){% endblock type %}

{% block value %}
    <intro>
    {% for item in var.value %}
        {{ render_type(item.value, item.key) }}
        {% if not item.value.composed %}<intro>{% endif %}
    {% endfor %}
{% endblock %}