{% extends "_base.console.twig" %}

{% block type %}
    <t_array_block>{{ var.title|tags|raw }}</t_array_block><intro>
{% endblock %}

{% block value %}
    {{ m.indent(var.level + 1) }}
    {{ var.data|replace({' ': '<space>', '\n': '<intro>' ~ m.indent(var.level + 1)})|raw }}
    <intro>
{% endblock %}