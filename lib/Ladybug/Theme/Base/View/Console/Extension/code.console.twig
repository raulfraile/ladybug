{% import "_macros.console.twig" as m %}

{% spaceless %}
{{ m.visibility_block(visibility) }}

{{ m.array_key(key) }}
{{ spaces_level(var.level)|raw }}
{{ var.data|replace({' ': '<space>', '\n': '<intro>' ~ spaces_level(var.level)})|raw }}
    {% endspaceless %}
