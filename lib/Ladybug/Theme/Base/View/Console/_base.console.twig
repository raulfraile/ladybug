{% import "_macros.console.twig" as m %}

{{ m.visibility_block(visibility) }}

{{ m.array_key(key) }}

{% block type %}{{ var.type }}<space>{% endblock %}

{% if var.type != 'null' %}
    {% if not var.composed %}<t_{{ var.type }}>{% endif %}
    {% block value %}{{ var.formattedValue()|raw }}{% endblock %}
    {% if not var.composed %}</t_{{ var.type }}>{% endif %}
{% endif %}
