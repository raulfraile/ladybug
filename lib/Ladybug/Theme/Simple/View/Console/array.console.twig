{% import "_macros.console.twig" as m %}

{% spaceless %}
    {{ m.visibility_block(visibility) }}

    {{ m.array_key(key) }}

    <t_array>array({{ var.length }})</t_array><intro>

        {% for item in var.value %}
            {{ m.indent(var.level + 1) }}
            {{ render_type(item.value, item.key) }}
            {% if not loop.last %}<intro>{% endif %}
        {% endfor %}


{% endspaceless %}
