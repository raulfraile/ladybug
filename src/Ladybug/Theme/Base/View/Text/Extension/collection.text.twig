{% extends "_base.text.twig" %}

{% block type %}{{ m.indent(level) }}{{ var.title|tags }}{% endblock type %}

{% block value %}
    <intro>
    {% for item in var.items %}
        {{ m.indent(item.level) }} {{ render_type(item, item.key) }}<intro>
    {% endfor %}
{% endblock %}
