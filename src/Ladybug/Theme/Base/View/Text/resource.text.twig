{% extends "_base.text.twig" %}

{% block type %}{{ var.type }}({{ var.resourceType|tags }}){% endblock type %}
{% block value %}

    {% if var.resourceCustomData is not empty %}
        <intro>
        {{ render_type(var.resourceCustomData) }}
    {% endif %}

{% endblock %}
