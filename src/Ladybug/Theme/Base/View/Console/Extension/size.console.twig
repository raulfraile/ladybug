{% import "_macros.console.twig" as m %}

{% spaceless %}
    {{ m.visibility_block(visibility) }}

    {{ m.array_key(key) }}
    {{ var.sizeUnit|number_format(2) }}
    <space>
    {{ var.unit }}
{% endspaceless %}
